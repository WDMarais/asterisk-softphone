{
  "name": "asterisk-softphone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser WebRTC softphone — a product consumer of the asterisk-sandbox broker API",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3",
    "vite": "^8.1.0",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "sip.js": "^0.21.2"
  },
  "engines": {
    "node": ">=20"
  }
}
